{
  "buildCommand": "cd rizviz-frontend && npm install --loglevel=error && CI=false npm run build",
  "outputDirectory": "rizviz-frontend/build",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
