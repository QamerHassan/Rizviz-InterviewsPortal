# Frontend environment variables for Railway deployment
# Copy these values into your Railway frontend service environment variables.

# Must point to your Railway API URL (without trailing slash)
REACT_APP_API_URL=https://your-api-service.up.railway.app/api

# Optional: Override PORT if Railway provides PORT automatically
PORT=3000
